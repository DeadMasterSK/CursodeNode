const nombre = 'DeadPool';
const real = 'Wade Winstone'

const normal = nombre + ' ' + real;
const template = `El nombre es ${nombre} y el personal es ${real}`;
console.log(normal);
console.log(template);

const html = `<h1>Hola</h1>
<p>Mundo</p>`;

console.log(html);